# dispatcher-sandbox

Minimal Node.js sandbox project.

## Getting started

```bash
npm install
npm start
```

## Layout

- `src/` — source code (entry: `src/index.js`)
- `package.json` — project manifest
